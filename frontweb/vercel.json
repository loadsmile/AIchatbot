{
  "buildCommand": "cd frontweb && yarn && yarn build",
  "outputDirectory": "frontweb/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "VITE_SOCKET_SERVER_URL": "https://translator-n1tr.onrender.com"
  }
}